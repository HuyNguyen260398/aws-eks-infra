extends: default

ignore: |
  gitops/platform/charts/namespace-config/templates/

rules:
  document-start: disable
  line-length:
    max: 120
    level: warning
  truthy:
    allowed-values: ["true", "false"]
    check-keys: false
