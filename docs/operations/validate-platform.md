# Validate the Platform

Run `make terraform-check`, `make yaml-check`, `./scripts/verify-prerequisites.sh`, `./scripts/verify-platform.sh`, and `./scripts/check-drift.sh`. Every command must exit zero.
