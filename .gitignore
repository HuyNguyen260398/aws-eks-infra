**/.terraform/*
*.tfstate
*.tfstate.*
*.tfplan
tfplan
*.tflock
# Child modules are validated with `init -backend=false`, which writes a lock
# file. Only the deployable roots pin providers.
modules/*/.terraform.lock.hcl
backend.hcl
terraform.tfvars
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.DS_Store
.idea/
.vscode/
